{
  "name": "arxiv-browser",
  "version": "2.0.0",
  "description": "arXiv paper browser — search, library, BibTeX, AI",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:win": "electron-packager . \"arXiv Browser\" --platform=win32 --arch=x64 --icon=assets/icon256.png --out=dist --overwrite --asar"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-packager": "^17.1.2"
  }
}